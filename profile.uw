# ~/.profile.local
# Created by Spencer Bliven
# 
# This file provides settings specific to dante.u.washington.edu

export PATH="$HOME/mysql/bin:$PATH"
export MANPATH="$HOME/mysql/man:$MANPATH"

#last, start up screen
if [[ ! $STY && -x `which screen` ]]; then
    screen -xRR
fi

